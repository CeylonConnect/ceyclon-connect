import React from "react";

export default function UsersPanel() {
  // Placeholder — connect to your real admin users endpoint if available
  return (
    <div className="mt-6 rounded-2xl border border-neutral-200 bg-white p-6 text-neutral-600">
      Users management can be added here (search, roles, block/unblock).
    </div>
  );
}